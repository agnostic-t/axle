{
  "metadata": {
    "name": "Main module",
    "version": "0.0.0"
  },

  "compiler": {
    "binary": "executable"
  },

  "code": {
    "sources": ["*.c", "**/*.c"],
    "includes": ["include"],
    "output": "./bin/main"
  },

  "dependencies": {
    "base": { "version": ">=0.0.0" }
  },

  "target": "debug",
  "defaults": "defaults.json"
}
